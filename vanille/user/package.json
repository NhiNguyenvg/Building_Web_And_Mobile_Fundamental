{
  "name": "user",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "live-server --port=8080 --watch=dist,index.html"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "live-server": "^1.2.2"
  }
}